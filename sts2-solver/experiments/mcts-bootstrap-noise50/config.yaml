name: mcts-bootstrap-noise50
method: mcts_selfplay
description: 'Bootstrap + Dirichlet noise frac 0.5 (vs default 0.25): force more
  exploration away from the policy prior at the root. Targets the echo-chamber
  pattern where MCTS visits echo a sharp prior (Strike ~60% / Prepared ~20%) and
  the policy trains itself back into the same shape. Cold start.'
created: '2026-04-17T00:28:56+00:00'
parent: mcts-bootstrap-v1
parent_checkpoint: latest
architecture:
  arch_version: 2
  base_state_dim: 137
  hidden_dim: 128
  action_hidden: 64
  hand_proj_dim: 32
  card_embed_dim: 16
  card_stats_dim: 28
  relic_dim: 26
  action_dim: 35
  max_hand: 10
  max_actions: 30
  num_cards: 578
  total_params: 72371
  state_dim: 427
  trunk_input: 169
training:
  generations: 1500
  combats_per_gen: 256
  lr: 0.0003
  batch_size: 512
  mcts:
    num_sims: 400
    temperature: 1.0
    replay_capacity: 50000
    train_epochs: 4
    value_coef: 1.0
    turn_boundary_eval: true
    c_puct: 1.5
    pomcp: true
    mcts_bootstrap: true
    noise_frac: 0.5
data:
  mode: encounter_set
  encounter_set: lean-decks-v1
checkpoints:
  save_every: 50
  keep_best: true
  cold_start: true
